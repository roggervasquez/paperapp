# Use an official Nginx image
FROM nginx:alpine

# Copy static files from frontend directory to the default nginx html directory
COPY . /usr/share/nginx/html

# Expose port 80
EXPOSE 80

# Nginx will start automatically
